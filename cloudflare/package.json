{
  "name": "redvia-tracking-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply redvia_tracking --local",
    "migrate:remote": "wrangler d1 migrations apply redvia_tracking --remote",
    "tokens:create": "node scripts/create-token.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
